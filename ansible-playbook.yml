---
- name: kubeCodeDeploy
  hosts: kube
  become: true
  tasks:
    - name: Create the namespace
      shell: kubectl create -f mynamespace.yaml

    - name: Create the svc
      shell: kubectl create -f mysvc.yaml -n mypod

    - name: Create the mydeployment
      shell: kubectl create -f mydeployment.yaml -n mypod

